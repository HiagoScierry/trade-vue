const state = {
    enterprises: [],
  };
  
  const getters = {
    enterprises: (state) => state.enterprises,
  };
  
  const actions = {
  };
  
  const mutations = {
  };
  
  export default {
    state,
    getters,
    actions,
    mutations,
  };
  